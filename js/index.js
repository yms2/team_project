const slides = document.querySelector('.main');
const slideImg = document.querySelectorAll('.main img')

slideWidth = 1920;
slideMargin = 0;

function move(){

    let curIndex = 0;

    setInterval(function(){
        slides.style
    })
}